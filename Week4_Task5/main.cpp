#include <iostream>
#include <stdio.h>

using namespace std;

int main()
{
    int numCustomers[5][4];

    for(int ry = 0 ; ry < 5; ry++)
    {
        for (int kol = 0; kol < 4; kol++);
        numCustomers[ry][kol] = rand(range(0,100))
    }

    cout << "Hello world!" << endl;
    return 0;
}
